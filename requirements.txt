# ─────────────────────────────────────────────────────────────────────────────
# RageBot MCP — Core Requirements
# Install with:  pip install -r requirements.txt
# Or via pip:    pip install ragebot-mcp
# ─────────────────────────────────────────────────────────────────────────────

# CLI framework
typer[all]>=0.9.0
rich>=13.0.0

# Embeddings for semantic search (downloads ~90 MB model on first use)
sentence-transformers>=2.7.0

# Token counting
tiktoken>=0.5.0

# Secure API key storage (macOS Keychain / GNOME Keyring / Windows Credential Manager)
keyring>=24.0.0

# .env file support
python-dotenv>=1.0.0

# Document summarisation (extractive)
nltk>=3.8.0

# Interactive prompts
questionary>=2.0.0
